<Page
    x:Class="TwitterUniversalApp.Views.TimeLine"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TwitterUniversalApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    mc:Ignorable="d"
    xmlns:vm="using:TwitterUniversalApp.ViewModels">

    <Page.DataContext>
        <vm:TimeLinePageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <ScrollViewer>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
<<<<<<< HEAD
        <StackPanel Orientation="Vertical">
                <Image Name="ImageBandeau" Source="{Binding Selecteduser.ProfileBannerURL}"></Image>
                <StackPanel Orientation="Horizontal">
                    <Image Source="{Binding Selecteduser.ProfileImageUrl400x400}" Width="250" Height="250" HorizontalAlignment="Left"/>
                    <StackPanel Orientation="Horizontal" >
                        <Button />
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Tweets : "/>
                            <TextBlock Text="{Binding Selecteduser.StatusesCount}"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Followed : "/>
                            <TextBlock Text="{Binding Selecteduser.FavouritesCount}"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Followers : "/>
                            <TextBlock Text="{Binding Selecteduser.FollowersCount}"/>
                        </StackPanel>
                    </StackPanel>
=======
        <controls:PageHeader x:Name="pageHeaderConnection" Content="Twitter">
            <!--  place stretched, across top  -->
            <RelativePanel.AlignTopWithPanel>True</RelativePanel.AlignTopWithPanel>
            <RelativePanel.AlignRightWithPanel>True</RelativePanel.AlignRightWithPanel>
            <RelativePanel.AlignLeftWithPanel>True</RelativePanel.AlignLeftWithPanel>
        </controls:PageHeader>
        <Canvas Margin="0,50,0,900" Height="250">
            <StackPanel Orientation="Vertical" Width="1000" Height="250">
                <Image Name="ImageBandeau" Width="1000" Height="200"></Image>
                <StackPanel Orientation="Horizontal" Width="500" Height="50">
                    <Button Name="ButtonTweets" Width="70" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"></Button>
                    <Button Name="ButtonAbonnements" Width="70" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"></Button>
                    <Button Name="ButtonAbonnees" Width="70" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"></Button>
                    <Button Name="ButtonAimes" Width="70" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"></Button>
                    <Button Name="ButtonSuivre" VerticalAlignment="Center" Margin="130,0" Width="90" Height="50">
                    </Button>
>>>>>>> origin/master
                </StackPanel>
            <StackPanel Orientation="Vertical">
                <TextBlock Name="ProfileName" Text="{Binding Selecteduser.Name}"/>
                <TextBlock Name="ProfilePseudo" Text="{Binding Selecteduser.ScreenName}"/>
                <TextBlock Name="ProfileBio" Text="{Binding Selecteduser.Description}"/>
                <TextBlock Name="ProfileLocation" Text="{Binding Selecteduser.Location}"/>
                <TextBlock Name="ProfileUrl" Text="{Binding Selecteduser.Url}"/>
                <TextBlock Name="ProfileJoinDate" Text="{Binding Selecteduser.CreatedAt}"/>
                <!--<TextBox Name="ProfileBirthday" Text="{Binding Loggeduser}"/>
                <TextBox Name="ProfileNumberPicture"/>
                <Grid Margin="0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="100"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <Image Name="ProfilePictureFirst"></Image>
                    <Image Name="ProfilePictureSecond" Grid.Row="0" Grid.Column="1"/>
                    <Image Name="ProfilePictureThrid" Grid.Row="0" Grid.Column="2"/>
                    <Image Name="ProfilePictureFourth" Grid.Row="1" Grid.Column="0"/>
                    <Image Name="ProfilePictureFifth" Grid.Row="1" Grid.Column="1"/>
                    <Image Name="ProfilePictureSixth" Grid.Row="1" Grid.Column="2"/>
                </Grid>-->
        </StackPanel>
        <StackPanel Orientation="Vertical" Width="500" Margin="340,350,50,0">
<<<<<<< HEAD
            <ListView Name="TimeLineList" ItemsSource="{Binding TimeLineTweets}" SelectedItem="" Height="700" Width="500">
=======
            <ListView Name="TimeLineTweeter" ItemsSource="" SelectedItem="" Height="700" Width="500">
>>>>>>> origin/master
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <Image Name="TimeLineProfileTweetImage"/>
                                <TextBlock Name="TimeLineProfileName" Text="{Binding CreatedBy.Name}"/>
                                <TextBlock Name="TimeLineProfilePseudo" Text="{Binding CreatedBy.ScreenName}"/>
                                <TextBlock Name="TimeLinePostedSince" Text="{Binding CreatedAt}"/>
                            </StackPanel>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Name="TimeLineTweetText" Text="{Binding Text}"/>
                                <Image Name="TimeLineTweetImage"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
<<<<<<< HEAD
                                <Button Name="TimeLineReply" Content="Répondre"/>
                                <Button Name="TimeLineRetweet" Content="{Binding RetweetCount}"/>
                                <Button Name="TimeLineFavorite" Content=""/>
                                <Button Name="TimeLine"/>
                                <ComboBox x:Name="comboBox1" Width="100">
=======
                                <Button Name="TimeLineReply"></Button>
                                <Button Name="TimeLineRetweet"></Button>
                                <Button Name="TimeLineFavorite"></Button>
                                <Button Name="TimeLine"></Button>
                                <ComboBox x:Name="comboBox" ItemsSource="{Binding MyItems}" SelectedItem="{Binding MySelectedItem}" Width="100">
>>>>>>> origin/master
                                    <x:String>Copier le lien du Tweet</x:String>
                                    <x:String>Intégrer le Tweet</x:String>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Page>
